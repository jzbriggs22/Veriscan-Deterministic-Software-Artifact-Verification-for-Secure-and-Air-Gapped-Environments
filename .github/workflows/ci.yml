name: CI

on:
  push:
    branches: ["main"]
  pull_request:

env:
  CARGO_TERM_COLOR: always

jobs:
  rust:
    name: Rust (veriscan)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # rustup resolves the toolchain (and rustfmt/clippy components)
      # from rust-toolchain.toml automatically.
      - name: Show toolchain
        run: rustup show

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: cargo-${{ runner.os }}-${{ hashFiles('Cargo.lock', 'rust-toolchain.toml') }}

      - name: Format check
        run: cargo fmt --check

      - name: Clippy (deny warnings)
        run: cargo clippy --all-targets -- -D warnings

      - name: Tests
        run: cargo test

      # Advisory for now: the lockfile has not been audited historically,
      # so a pre-existing advisory must not block unrelated changes.
      - name: Dependency audit
        continue-on-error: true
        run: |
          cargo install cargo-audit --locked
          cargo audit

  python:
    name: Python (ai-governance-mvp)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ai-governance-mvp
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: ai-governance-mvp/requirements.txt

      - name: Install dependencies
        run: pip install -r requirements.txt

      # pytest.ini enforces the 90% coverage gate via addopts.
      - name: Tests with coverage gate
        run: python -m pytest
